# PostgreSQL is installed on the same Debian VM as the app.
DB_HOST=127.0.0.1
DB_PORT=5432
DB_NAME=irur
DB_USER=irur_app
DB_PASSWORD=replace-with-your-local-postgres-password
JWT_SECRET=replace-with-a-long-random-secret
